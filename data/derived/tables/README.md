# Table source data

This directory contains the machine-readable files underlying the numbered manuscript tables.

Current files:

- Table1_source_data.csv
- Table2_source_data.csv

If later revisions split or expand a table, add a short companion note describing the aggregation logic.
